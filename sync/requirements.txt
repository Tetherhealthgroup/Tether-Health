# Runtime dependencies, pinned to the versions this service was built and
# tested against on 2026-09-15 (Python 3.14.6).
#
# Pinned exactly rather than with ranges: a sync service for health records
# should fail at build time when a dependency moves, not at 3am when pydantic
# changes how it renders a validation error and the redaction in
# thsync/problems.py stops matching.
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.5
pydantic_core==2.46.5
SQLAlchemy==2.0.53
PyJWT==2.14.0
uvicorn==0.53.0
